import './SassCard.scss'; // Import the Sass stylesheet

function SassCard() {
  return (
    <div className="card-sass">
      <h3>Styled with Sass</h3>
      <p>This card uses `.scss` for variables and nesting.</p>
    </div>
  );
}

export default SassCard;